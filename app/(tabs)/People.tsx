import React, { useState } from "react";
import { StyleSheet, View } from "react-native";

const Profile = () => {
  return <View style={styles.container}>people</View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default Profile;
